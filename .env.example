# Claude API key — enables AI parsing of forwarded fee note emails
# (/api/parse-fee-note). Without it the app falls back to a deterministic
# local parser; everything else works unchanged.
ANTHROPIC_API_KEY=

# Supabase (production data layer — not yet wired; schema in supabase/migrations)
# NEXT_PUBLIC_SUPABASE_URL=
# NEXT_PUBLIC_SUPABASE_ANON_KEY=
